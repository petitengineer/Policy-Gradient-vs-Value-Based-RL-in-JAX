Student: Jonathan Horton #7710257

# Executing Code
The code requires the use of Python 3.11.0. Later versions of Python may or may not work as well. Assuming this version of Python is installed, execute:

```bash
py -3.11 -m venv myvenv #Creates venv named myvenv from the 3.12 installation
.\myvenv\Scripts\activate #Activate.ps1 for powershell, use "activate" for bash on Linux/Mac
pip install -r requirements.txt # Make sure you are in same directory as the requirements file
```

If you want to run the code with pits, simply open the jupyter notebook and set RUN_WITH_PITS = True. By default it is false.

# Report
Please see the provided .pdf file (as requrested) for answers to questions.

# AI Disclosure
Please see the note appended at the end of the report.# Policy-Gradient-vs-Value-Based-RL-in-JAX
